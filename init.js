/* eslint-disable no-undef */
import Config from "./config/configs.js";

//Initializes the phaser game with the imported config 
// eslint-disable-next-line no-unused-vars
const game = new Phaser.Game(Config);